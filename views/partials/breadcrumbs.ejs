<nav class="breadcrumbs" aria-label="Breadcrumb" style="font-size: 0.75rem; color: var(--text-muted); display: flex; align-items: center; gap: 0.5rem; font-weight: 500; text-transform: uppercase; letter-spacing: 0.05em; padding: 0.25rem 0;">
    <a href="/" style="color: inherit; text-decoration: none; transition: color 0.2s;">Home</a>
    <span style="color: var(--text-muted); opacity: 0.5;">/</span>
    <span class="breadcrumb-active" style="color: var(--color-accent, #8b5cf6);"><%= typeof pageTitle !== 'undefined' ? pageTitle : 'Workspace' %></span>
</nav>
